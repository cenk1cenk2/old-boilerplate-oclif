export enum LogLevels {
  silent = 'silent',
  direct = 'direct',
  fatal = 'fatal',
  fail = 'fail',
  warn = 'warn',
  success = 'success',
  info = 'info',
  module = 'module',
  verbose = 'verbose',
  debug = 'debug'
}

export enum LoggerConstants {
  DEFAULT_LOGGER = 'default'
}